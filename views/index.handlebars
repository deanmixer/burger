<div class="jumbotron">
  <h1 class="display-4"><span class="goodb">Good</span> <span class="gburger">Burger</span></h1>
  <p class="lead">Welcome to Good Burger, the home of the Good Burger. Can we take your order?</p>
</div>

<div class="container menu">
  <div class="row">
    <div class="col-md-12">
      <h4>Don't see a burger you like below? Add your own here:</h4>
      <form class="create-form">
        <div class="form-group">
          {{!-- <label for="ca">Burger name:</label> --}}
          <input type="text" id="ca" name="name" class="form-control" required maxlength="40">
        </div>
        <button type="submit" class="btn btn-warning">Add Burger</button>
      </form>
    </div>
  </div>
  <div class="row">
    <div class="col-sm-12 col-md-6">
      <h4>Burgers to eat</h4>
      <ul class="list-group">
        {{#each burgers}}
        {{#unless devoured}}
          {{> burgers/burger-block devoured=true}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>
    <div class="col-sm-12 col-md-6">
      <h4>Eaten burgers</h4>
      <ul class="list-group">
        {{#each burgers}}
        {{#if devoured}}
          {{> burgers/burger-block devoured=false}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>
</div>